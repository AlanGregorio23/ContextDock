@php
    $navigation = ['dashboard' => 'Panoramica', 'workspaces' => 'Workspace', 'projects' => 'Progetti', 'memories' => 'Memorie', 'conversations' => 'Conversazioni', 'documents' => 'Documenti', 'inspector' => 'Context Inspector', 'integrations' => 'Integrazioni', 'jobs' => 'Indicizzazione', 'logs' => 'Registro attività', 'backups' => 'Backup', 'settings' => 'Configurazione'];
    $descriptions = ['dashboard' => 'La memoria dei tuoi progetti. Indipendente dalle app AI.', 'workspaces' => 'Organizza progetti e conoscenza in spazi separati.', 'projects' => 'Un perimetro preciso per ogni progetto.', 'memories' => 'Conserva ciò che conta, con uno scope esplicito.', 'conversations' => 'Archivia messaggi originali inviati esplicitamente a ContextDock.', 'documents' => 'Trasforma i tuoi file in conoscenza recuperabile.', 'inspector' => 'Esamina ricerca, ranking e contesto prima di usarli.', 'integrations' => 'Collega le app AI alla tua memoria tramite MCP.', 'jobs' => 'Controlla l’indicizzazione locale di memorie e documenti.', 'logs' => 'Le operazioni registrate per il tuo account.', 'backups' => 'Richiedi una copia e controlla l’esito delle operazioni.', 'settings' => 'Le fondamenta della tua installazione self-hosted.'];
@endphp
<div class="dock-shell" x-data="{ menuOpen: false }">
    <a class="skip-link" href="#main-content">Vai al contenuto</a>
    <aside class="sidebar" :class="{ 'sidebar-open': menuOpen }" aria-label="Navigazione principale">
        <a href="/dashboard" class="brand" wire:navigate><span class="brand-mark" aria-hidden="true">C<span>·</span></span><span>ContextDock<small>IL TUO CONTEXT ENGINE</small></span></a>
        <div class="sidebar-label">LIBRERIA PERSONALE</div>
        <nav>
            @foreach($navigation as $key => $label)
                @if($key === 'integrations')<div class="sidebar-label nav-divider">SISTEMA</div>@endif
                <a href="/{{ $key }}" wire:navigate @class(['nav-link', 'active' => $section === $key]) @if($section === $key) aria-current="page" @endif>
                    <span class="nav-dot" aria-hidden="true"></span>{{ $label }}
                    @if($key === 'inspector')<span class="nav-tag">CTX</span>@endif
                </a>
            @endforeach
        </nav>
        <div class="sidebar-bottom"><span class="local-dot" aria-hidden="true"></span> Embedding locale<small>qwen3-embedding:0.6b · Ollama</small></div>
    </aside>
    <div class="main-shell">
        <header class="topbar">
            <button class="icon-button mobile-menu" type="button" @click="menuOpen = !menuOpen" :aria-expanded="menuOpen" aria-label="Apri o chiudi menu">☰</button>
            <div class="breadcrumb">ContextDock <span>/</span> <strong>{{ $navigation[$section] }}</strong></div>
            <div class="account"><span class="avatar" aria-hidden="true">{{ mb_strtoupper(mb_substr(auth()->user()->name, 0, 1)) }}</span><span class="account-name">{{ auth()->user()->name }}</span><form action="/logout" method="POST">@csrf<button class="text-button" type="submit">Esci</button></form></div>
        </header>
        <main id="main-content" class="main-content" tabindex="-1">
            <div class="page-heading"><div><div class="eyebrow">{{ in_array($section, ['integrations', 'jobs', 'logs', 'backups', 'settings']) ? 'CONTROLLO LOCALE' : 'LA TUA CONOSCENZA' }}</div><h1>{{ $navigation[$section] }}</h1><p>{{ $descriptions[$section] }}</p></div>@if($section === 'dashboard')<a class="button primary" href="/inspector" wire:navigate>Apri Context Inspector <span aria-hidden="true">↗</span></a>@endif</div>

            @if($notice)<div class="notice" role="status">{{ $notice }}<button type="button" class="text-button" wire:click="$set('notice', '')" aria-label="Chiudi notifica">×</button></div>@endif
            @if($errors->any())<div class="error-box" role="alert"><strong>Controlla l’operazione</strong><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
            <div wire:loading.delay class="loading-status" role="status">Operazione in corso…</div>

            @if(in_array($section, ['memories', 'conversations', 'documents', 'inspector']))
                <div class="scope-bar"><label for="active-project">Progetto attivo</label><select id="active-project" wire:model.live="projectId"><option value="">{{ in_array($section, ['memories', 'documents', 'conversations']) ? 'Tutti i miei progetti' : 'Seleziona un progetto' }}</option>@foreach($projects as $project)<option value="{{ $project->id }}">{{ $project->name }}</option>@endforeach</select><span class="muted">Accesso limitato al tuo account</span></div>
            @endif

            @if($section === 'dashboard')
                <div class="stats-grid">@foreach($stats as $label => $value)<div class="stat-card"><span>{{ $label }}</span><strong>{{ number_format($value, 0, ',', '.') }}</strong><small>Nel tuo account</small></div>@endforeach</div>
                <section class="hero-panel"><div class="hero-copy"><span class="eyebrow">MEMORIA PERSISTENTE, AI INDIPENDENTI</span><h2>Il contesto resta tuo.<br>Le app AI possono cambiare.</h2><p>Salva memorie, indicizza documenti e prepara Context Pack verificabili. Le app collegate recuperano solo il contesto autorizzato.</p><div class="button-row"><a href="/memories" wire:navigate class="button primary">Gestisci le memorie</a><a href="/integrations" wire:navigate class="button secondary">Configura MCP</a></div></div><div class="context-flow" aria-label="Flusso: app AI, ContextDock, ricerca locale"><div class="flow-node">App AI esterna<small>ChatGPT · Claude · altre app</small></div><span class="flow-arrow" aria-hidden="true">↓ MCP / skill / adapter</span><div class="flow-node emphasized">ContextDock<small>Permessi · Retrieval · Context Pack</small></div><span class="flow-arrow" aria-hidden="true">↓ ricerca locale</span><div class="flow-node">PostgreSQL + pgvector<small>Ollama · qwen3-embedding:0.6b</small></div></div></section>
                <div class="two-column"><section class="panel"><div class="panel-heading"><h2>I tuoi progetti</h2><a href="/projects" wire:navigate class="text-link">Gestisci →</a></div>@forelse($projects->take(5) as $project)<a href="/memories" wire:navigate class="list-row"><span class="project-icon" aria-hidden="true">▧</span><div><strong>{{ $project->name }}</strong><small>{{ \Illuminate\Support\Str::limit($project->description ?: 'Nessuna descrizione', 90) }}</small></div><span aria-hidden="true">↗</span></a>@empty<div class="empty-state"><span class="empty-symbol" aria-hidden="true">＋</span><h3>Il primo progetto parte da qui</h3><p>Crea un workspace, poi aggiungi il tuo progetto.</p><a href="/workspaces" wire:navigate class="button secondary">Crea workspace</a></div>@endforelse</section><section class="panel"><div class="panel-heading"><h2>Attività recente</h2><a href="/logs" wire:navigate class="text-link">Tutte →</a></div>@forelse($rows as $entry)<div class="list-row"><span class="activity-dot" aria-hidden="true"></span><div><strong>{{ $entry->action }}</strong><small>{{ $entry->created_at->format('d/m/Y H:i') }}</small></div></div>@empty<div class="empty-state"><h3>Nessuna attività registrata</h3><p>Le operazioni compariranno qui quando inizi a usare ContextDock.</p></div>@endforelse</section></div>

            @elseif($section === 'workspaces')
                <div class="two-column"><section class="panel"><div class="panel-heading"><h2>I tuoi workspace</h2><span class="badge">{{ $workspaces->count() }} mostrati</span></div>@forelse($workspaces as $workspace)<div class="list-row"><span class="project-icon" aria-hidden="true">▧</span><div><strong>{{ $workspace->name }}</strong><small>Creato {{ $workspace->created_at->format('d/m/Y') }}</small></div></div>@empty<div class="empty-state"><h3>Uno spazio per ogni ambito</h3><p>Ad esempio Personale, Lavoro o Clienti.</p></div>@endforelse</section><form wire:submit="createWorkspace" class="panel form-panel"><h2>Nuovo workspace</h2><p class="muted">Raggruppa progetti e memorie condivise nel tuo account.</p><label for="workspace-name">Nome</label><input id="workspace-name" wire:model="workspaceName" required maxlength="120" placeholder="Es. Laboratorio"><button class="button primary" wire:loading.attr="disabled">Crea workspace</button></form></div>

            @elseif($section === 'projects')
                <div class="two-column"><section class="panel"><div class="panel-heading"><h2>Progetti</h2><span class="badge">{{ $projects->count() }} mostrati</span></div>@forelse($projects as $project)<div class="list-row"><span class="project-icon" aria-hidden="true">▧</span><div><strong>{{ $project->name }}</strong><small>{{ $workspaces->firstWhere('id', $project->workspace_id)?->name }} · #{{ $project->id }}</small><p class="muted">{{ $project->description }}</p></div></div>@empty<div class="empty-state"><h3>Ogni progetto, il suo contesto</h3><p>Crea prima un workspace e poi aggiungi il progetto.</p></div>@endforelse</section><form wire:submit="createProject" class="panel form-panel"><h2>Nuovo progetto</h2><label for="project-workspace">Workspace</label><select id="project-workspace" wire:model="workspaceId" required><option value="">Seleziona workspace</option>@foreach($workspaces as $workspace)<option value="{{ $workspace->id }}">{{ $workspace->name }}</option>@endforeach</select><label for="project-name">Nome progetto</label><input id="project-name" wire:model="projectName" required maxlength="160" placeholder="Es. ContextDock"><label for="project-description">Descrizione</label><textarea id="project-description" wire:model="projectDescription" rows="4" placeholder="Obiettivo, perimetro e informazioni utili"></textarea><button class="button primary" wire:loading.attr="disabled">Crea progetto</button></form></div>

            @elseif($section === 'memories')
                <div class="content-with-form"><section class="panel"><div class="panel-heading"><h2>Libreria memorie</h2><span class="badge">{{ $rows->total() }}</span></div><div class="panel-toolbar"><label class="sr-only" for="memory-search">Cerca nel titolo e nel contenuto</label><input id="memory-search" type="search" wire:model.live.debounce.350ms="search" placeholder="Cerca nel titolo e nel contenuto…"><small class="muted">Filtro testuale. Usa Inspector per la ricerca semantica. Sono incluse le memorie senza progetto.</small></div>@forelse($rows as $row)<article class="memory-card" wire:key="memory-{{ $row->id }}"><div class="card-meta"><span class="badge">{{ $row->type }}</span><span>{{ $row->scope }}</span><span class="status-pill">{{ $row->embedding_status }}</span>@if($row->is_pinned)<span class="pin-label">In evidenza</span>@endif</div><h3>{{ $row->title }}</h3><p class="preserve-text">{{ \Illuminate\Support\Str::limit($row->content, 440) }}</p><div class="card-footer"><small>Importanza {{ $row->importance }} · Affidabilità {{ $row->confidence }}</small><div class="row-actions"><button type="button" wire:click="pinMemory({{ $row->id }})" aria-label="{{ $row->is_pinned ? 'Rimuovi pin da' : 'Metti in evidenza' }} {{ $row->title }}">{{ $row->is_pinned ? 'Unpin' : 'Pin' }}</button><button type="button" wire:click="editMemory({{ $row->id }})">Modifica</button><button type="button" class="danger-text" wire:click="deleteMemory({{ $row->id }})" wire:confirm="Eliminare definitivamente questa memoria?">Elimina</button></div></div></article>@empty<div class="empty-state"><span class="empty-symbol" aria-hidden="true">◇</span><h3>{{ $search ? 'Nessun risultato' : 'Conserva la prima memoria' }}</h3><p>{{ $search ? 'Prova un termine diverso o cambia il progetto attivo.' : 'Una decisione, una preferenza o un fatto utile al tuo progetto.' }}</p></div>@endforelse<div class="pagination">{{ $rows->links() }}</div></section>
                <form wire:submit="saveMemory" class="panel form-panel sticky-form"><div class="panel-heading"><h2>{{ $editingMemoryId ? 'Modifica memoria' : 'Nuova memoria' }}</h2>@if($editingMemoryId)<button type="button" class="text-button" wire:click="cancelEdit">Annulla</button>@endif</div><div class="field-grid"><div><label for="memory-scope">Scope</label><select id="memory-scope" wire:model.live="memory.scope"><option value="project">Progetto</option><option value="workspace">Workspace</option><option value="global">Globale</option><option value="session">Sessione</option></select></div><div><label for="memory-type">Tipo</label><select id="memory-type" wire:model="memory.type">@foreach(\App\Livewire\Dock::TYPES as $type)<option value="{{ $type }}">{{ $type }}</option>@endforeach</select></div></div>
                    @if($memory['scope'] === 'workspace')<label for="memory-workspace">Workspace</label><select id="memory-workspace" wire:model="workspaceId" required><option value="">Seleziona workspace</option>@foreach($workspaces as $workspace)<option value="{{ $workspace->id }}">{{ $workspace->name }}</option>@endforeach</select>@endif
                    @if($memory['scope'] === 'session')<label for="memory-conversation">Conversazione</label><select id="memory-conversation" wire:model="conversationId" required><option value="">Seleziona conversazione</option>@foreach($conversations as $conversation)<option value="{{ $conversation->id }}">{{ $conversation->title }}</option>@endforeach</select>@endif
                    <label for="memory-title">Titolo</label><input id="memory-title" wire:model="memory.title" required maxlength="255" placeholder="Es. Database principale"><label for="memory-content">Contenuto</label><textarea id="memory-content" wire:model="memory.content" required rows="6" placeholder="Scrivi un’informazione autonoma e riutilizzabile…"></textarea><label for="memory-summary">Sintesi facoltativa</label><textarea id="memory-summary" wire:model="memory.summary" rows="2"></textarea><div class="field-grid"><div><label for="memory-importance">Importanza (0–1)</label><input id="memory-importance" type="number" min="0" max="1" step="0.05" wire:model="memory.importance" required></div><div><label for="memory-confidence">Affidabilità (0–1)</label><input id="memory-confidence" type="number" min="0" max="1" step="0.05" wire:model="memory.confidence" required></div></div><label for="memory-expiry">Scadenza facoltativa</label><input id="memory-expiry" type="datetime-local" wire:model="memory.expires_at"><label class="checkbox-label"><input type="checkbox" wire:model="memory.is_pinned">Metti in evidenza nel Context Pack</label><button class="button primary" wire:loading.attr="disabled">{{ $editingMemoryId ? 'Salva modifiche' : 'Salva memoria' }}</button><small class="muted">L’embedding viene generato localmente da Ollama.</small></form></div>

            @elseif($section === 'conversations')
                <div class="content-with-form"><section class="panel"><div class="panel-heading"><h2>Archivio RAW</h2><span class="badge">Ultimi 50 messaggi</span></div><div class="panel-toolbar"><label for="active-conversation">Conversazione</label><select id="active-conversation" wire:model.live="conversationId">@forelse($conversations as $conversation)<option value="{{ $conversation->id }}">{{ $conversation->title }} ({{ $conversation->messages_count }} msg · {{ $conversation->source }})</option>@empty<option value="">Nessuna conversazione trovata</option>@endforelse</select></div><div class="message-list">@forelse($messages as $message)<article class="raw-message" wire:key="message-{{ $message->id }}"><div class="card-meta"><strong>{{ $message->role }}</strong><span>{{ $message->created_at->format('d/m/Y H:i') }}</span></div><p class="preserve-text">{{ $message->content }}</p></article>@empty<div class="empty-state"><h3>{{ $conversationId ? 'Nessun messaggio' : 'Apri o crea una conversazione' }}</h3><p>Le conversazioni delle app esterne vengono conservate solo quando le importi o le invii esplicitamente.</p></div>@endforelse</div></section><div class="form-stack"><form wire:submit="createConversation" class="panel form-panel"><h2>Nuova conversazione</h2><label for="conversation-title">Titolo</label><input id="conversation-title" wire:model="conversationTitle" required maxlength="255" placeholder="Es. Decisioni sul database"><button class="button secondary" wire:loading.attr="disabled">Crea conversazione</button></form>@if($conversationId)<form wire:submit="appendMessage" class="panel form-panel"><h2>Aggiungi messaggio RAW</h2><label for="message-role">Ruolo originale</label><select id="message-role" wire:model="messageRole"><option value="user">Utente</option><option value="assistant">Assistente</option><option value="system">Sistema</option></select><label for="message-content">Contenuto originale</label><textarea id="message-content" wire:model="messageContent" rows="8" required></textarea><button class="button primary" wire:loading.attr="disabled">Archivia messaggio</button><small class="muted">Aggiunta all’archivio, senza generazione AI o estrazione automatica di memorie.</small></form>@endif</div></div>

            @elseif($section === 'documents')
                <div class="content-with-form"><section class="panel"><div class="panel-heading"><h2>Documenti indicizzati</h2><span class="badge">{{ $rows->total() }}</span></div><div class="panel-toolbar"><label class="sr-only" for="document-search">Cerca un documento</label><input id="document-search" type="search" wire:model.live.debounce.350ms="search" placeholder="Cerca un documento…"></div>@forelse($rows as $row)<div class="list-row" wire:key="document-{{ $row->id }}"><span class="project-icon" aria-hidden="true">▤</span><div><strong>{{ $row->name }}</strong><small>{{ $row->created_at->format('d/m/Y H:i') }} · <span class="status-pill">{{ $row->status }}</span></small>@if($row->status === 'failed')<small class="danger-text">Indicizzazione non riuscita. Consulta i log del server.</small>@endif</div><button class="text-button danger-text" type="button" wire:click="deleteDocument({{ $row->id }})" wire:confirm="Eliminare il documento e tutti i suoi chunk?">Elimina</button></div>@empty<div class="empty-state"><span class="empty-symbol" aria-hidden="true">▤</span><h3>{{ $search ? 'Nessun documento trovato' : 'Dai una base documentale al progetto' }}</h3><p>Carica documentazione, note tecniche o codice sorgente.</p></div>@endforelse<div class="pagination">{{ $rows->links() }}</div></section><form wire:submit="storeDocument" class="panel form-panel"><h2>Carica documento</h2><p class="muted">Il file verrà suddiviso in chunk e indicizzato localmente nel progetto attivo.</p><label for="document-upload">File di testo · massimo 10 MB</label><input id="document-upload" type="file" wire:model="upload" required aria-describedby="upload-hint"><small id="upload-hint" class="muted">MVP: TXT, Markdown e codice testuale. PDF e DOCX saranno disponibili in una fase successiva.</small><div wire:loading wire:target="upload" role="status">Caricamento del file…</div><button class="button primary" wire:loading.attr="disabled">Carica e indicizza</button></form></div>

            @elseif($section === 'inspector')
                <section class="panel inspector-intro"><span class="badge accent">CONTEXT INSPECTOR</span><h2>Vedi esattamente cosa recupera ContextDock.</h2><p>Una query, uno scope e un budget. Ispeziona i candidati e il Context Pack che un’app AI può richiedere tramite MCP.</p></section><form wire:submit="buildContext" class="panel form-panel inspector-form"><label for="context-query">Query di ricerca semantica</label><textarea id="context-query" wire:model="query" rows="3" required placeholder="Quali decisioni architetturali abbiamo preso per questo progetto?"></textarea><div class="inspector-controls"><div><label for="context-conversation">Sessione facoltativa</label><select id="context-conversation" wire:model="conversationId"><option value="">Nessuna sessione</option>@foreach($conversations as $conversation)<option value="{{ $conversation->id }}">{{ $conversation->title }}</option>@endforeach</select></div><div><label for="context-limit">Risultati massimi</label><input id="context-limit" type="number" min="1" max="50" wire:model="resultLimit" required></div><div><label for="context-budget">Budget token stimati</label><input id="context-budget" type="number" min="256" max="32000" step="1" wire:model="budgetTokens" required></div><button class="button primary" wire:loading.attr="disabled"><span wire:loading.remove wire:target="buildContext">Genera Context Pack</span><span wire:loading wire:target="buildContext">Ricerca locale…</span></button></div></form>
                @if($contextResult)
                    <div class="result-meta"><span class="badge accent">Run #{{ $contextResult['run_id'] ?? '—' }}</span><span>Modello: {{ data_get($contextResult, 'debug.embedding.model', 'qwen3-embedding:0.6b') }}</span><span>Dimensioni: {{ data_get($contextResult, 'debug.embedding.dimensions', '—') }}</span><span>Token stimati: {{ data_get($contextResult, 'pack.metadata.estimated_tokens', data_get($contextResult, 'debug.estimated_tokens', '—')) }}</span></div><section class="panel"><div class="panel-heading"><h2>Candidati e ranking</h2><span class="badge">{{ count(data_get($contextResult, 'debug.candidates', [])) }} candidati</span></div><div class="table-scroll"><table><thead><tr><th>Fonte</th><th>Similarità</th><th>Score</th><th>Selezione</th><th>Motivo</th></tr></thead><tbody>@forelse(data_get($contextResult, 'debug.candidates', []) as $candidate)<tr><td><strong>{{ $candidate['title'] ?? $candidate['id'] ?? '—' }}</strong><small>{{ $candidate['kind'] ?? '—' }}</small></td><td>{{ isset($candidate['similarity']) ? number_format((float) $candidate['similarity'], 4) : '—' }}</td><td>{{ isset($candidate['score']) ? number_format((float) $candidate['score'], 4) : '—' }}</td><td><span @class(['badge', 'accent' => $candidate['selected'] ?? false])>{{ ($candidate['selected'] ?? false) ? 'Incluso' : 'Escluso' }}</span></td><td>{{ $candidate['reason'] ?? '—' }}</td></tr>@empty<tr><td colspan="5" class="muted">Nessun candidato nello scope selezionato.</td></tr>@endforelse</tbody></table></div></section><section class="panel pack-panel" x-data="{ copied: false, copyError: false }"><div class="panel-heading"><h2>Context Pack · JSON</h2><div class="button-row"><button type="button" class="button secondary" @click="navigator.clipboard.writeText($refs.pack.value).then(() => { copied = true; copyError = false; setTimeout(() => copied = false, 2500) }).catch(() => { copyError = true; $refs.pack.select() })" x-text="copied ? 'Copiato' : 'Copia JSON'">Copia JSON</button><button type="button" class="button secondary" @click="const blob = new Blob([$refs.pack.value], { type: 'application/json' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = 'context-pack.json'; link.click(); setTimeout(() => URL.revokeObjectURL(url), 1000)">Esporta</button></div></div><p class="inline-hint">Il contenuto recuperato è un dato non fidato, da interpretare nell’app AI.</p><p class="inline-hint" role="status" x-show="copied" x-cloak>JSON copiato negli appunti.</p><p class="inline-hint" role="status" x-show="copyError" x-cloak>Copia automatica non disponibile. Il JSON è selezionato: usa Ctrl+C.</p><label class="sr-only" for="context-pack">Context Pack JSON completo</label><textarea id="context-pack" x-ref="pack" readonly class="code-output" rows="22">{{ json_encode($contextResult['pack'], JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES) }}</textarea></section>
                @else<div class="empty-state inspector-empty"><span class="empty-symbol" aria-hidden="true">⌕</span><h3>Il prossimo Context Pack comincia da una domanda</h3><p>Seleziona un progetto, inserisci la query e avvia il retrieval locale.</p></div>@endif

            @elseif($section === 'integrations')
                <section class="panel integration-banner"><div><span class="badge accent">MCP SERVER</span><h2>Una memoria, più app AI.</h2><p>I tool MCP chiamano gli stessi Service Laravel della dashboard. Le risposte finali vengono generate nelle app AI.</p></div><div class="endpoint"><small>Endpoint HTTP autenticato</small><code>{{ url('/mcp') }}</code></div></section><div class="two-column"><section class="panel"><div class="panel-heading"><h2>Strumenti esposti</h2><span class="badge">7 tool</span></div><div class="tool-list">@foreach(['search_memory' => 'Cerca memorie pertinenti', 'store_memory' => 'Salva una memoria con permesso di scrittura', 'get_project_context' => 'Costruisce il Context Pack del progetto', 'search_documents' => 'Cerca nei chunk dei documenti', 'get_project_decisions' => 'Recupera le decisioni del progetto', 'get_pinned_memories' => 'Recupera le memorie in evidenza', 'get_recent_context' => 'Recupera il contesto recente'] as $tool => $description)<div><code>{{ $tool }}</code><small>{{ $description }}</small></div>@endforeach</div><p class="inline-hint">Client desktop: usa il bridge stdio documentato in <code>integrations/</code>. I connector cloud richiedono un endpoint raggiungibile e autenticazione compatibile; localhost non è raggiungibile dalle app cloud.</p></section><form wire:submit="createToken" class="panel form-panel"><h2>Nuovo token ContextDock</h2><p class="muted">Autorizza un client ad accedere al tuo account. Scade dopo 30 giorni.</p><label for="token-name">Nome del client</label><input id="token-name" wire:model="tokenName" required maxlength="80" placeholder="Es. Claude Desktop"><label class="checkbox-label"><input type="checkbox" wire:model="tokenWrite">Consenti anche il salvataggio di memorie</label><small class="muted">La lettura è sempre inclusa. Puoi revocare l’accesso in qualsiasi momento.</small><button class="button primary" wire:loading.attr="disabled">Genera token</button>@if($newToken)<div class="token-reveal"><label for="new-token">Copia e conserva questo token</label><textarea id="new-token" rows="3" readonly autocomplete="off" spellcheck="false">{{ $newToken }}</textarea><small>Trattalo come una password. Mostrato solo in questa pagina.</small><button type="button" class="button secondary" wire:click="dismissToken">Ho copiato il token · nascondi</button></div>@endif</form></div><section class="panel"><div class="panel-heading"><h2>Token del tuo account</h2></div><div class="table-scroll"><table><thead><tr><th>Client</th><th>Permessi</th><th>Scadenza</th><th>Ultimo utilizzo</th><th><span class="sr-only">Azioni</span></th></tr></thead><tbody>@forelse($rows as $token)<tr><td>{{ $token->name }}</td><td>{{ implode(', ', $token->abilities ?? []) }}</td><td>{{ $token->expires_at?->format('d/m/Y H:i') ?? 'Nessuna' }}</td><td>{{ $token->last_used_at?->format('d/m/Y H:i') ?? 'Mai' }}</td><td><button type="button" class="text-button danger-text" wire:click="revokeToken({{ $token->id }})" wire:confirm="Revocare questo token? Il client perderà subito l’accesso.">Revoca</button></td></tr>@empty<tr><td colspan="5" class="muted">Nessun token creato. Genera il primo per configurare un client.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $rows->links() }}</div></section>

            @elseif($section === 'jobs')
                <div class="two-column"><section class="panel"><div class="panel-heading"><h2>Memorie da indicizzare</h2><button type="button" class="button secondary" wire:click="$refresh">Aggiorna</button></div><p class="inline-hint">Stato dei tuoi record. Le memorie pronte sono disponibili nella libreria.</p>@forelse($rows as $row)<div class="list-row" wire:key="index-{{ $row->id }}"><div><strong>{{ $row->title }}</strong><small>{{ $row->embedding_status }} · {{ $row->updated_at->format('d/m/Y H:i') }}</small></div><button type="button" class="text-button" wire:click="reindexMemory({{ $row->id }})" wire:loading.attr="disabled">Reindicizza</button></div>@empty<div class="empty-state"><h3>Nessuna memoria in attesa</h3><p>Non risultano memorie del tuo account con indicizzazione incompleta.</p></div>@endforelse<div class="pagination">{{ $rows->links() }}</div></section><section class="panel"><div class="panel-heading"><h2>Stato documenti</h2></div>@forelse($stats as $status => $total)<div class="list-row"><strong>{{ $status }}</strong><span class="badge">{{ $total }}</span></div>@empty<div class="empty-state"><h3>Nessun documento</h3><p>Carica un file per avviare l’indicizzazione.</p><a href="/documents" wire:navigate class="button secondary">Apri documenti</a></div>@endforelse<p class="inline-hint">L’esecuzione dei job richiede il worker Redis e Ollama. Gli stati qui mostrati provengono dai record applicativi.</p></section></div>

            @elseif(in_array($section, ['logs', 'backups']))
                @if($section === 'backups')<section class="panel integration-banner"><div><span class="badge accent">BACKUP SELF-HOSTED</span><h2>Una copia da verificare, un ripristino da pianificare.</h2><p>Il backup viene eseguito in coda. Verifica l’esito nel registro e consulta la procedura di ripristino nella documentazione del repository.</p></div><button type="button" class="button primary" wire:click="dispatchBackup" wire:loading.attr="disabled">Richiedi backup</button></section>@endif
                <section class="panel"><div class="panel-heading"><h2>{{ $section === 'backups' ? 'Registro backup' : 'Audit del tuo account' }}</h2><button type="button" class="button secondary" wire:click="$refresh">Aggiorna</button></div>@if($section === 'logs')<div class="panel-toolbar"><label class="sr-only" for="audit-search">Filtra azioni</label><input id="audit-search" type="search" wire:model.live.debounce.350ms="search" placeholder="Filtra per azione…"></div>@endif<div class="table-scroll"><table><thead><tr><th>Data</th><th>Azione</th><th>Oggetto</th></tr></thead><tbody>@forelse($rows as $entry)<tr><td class="nowrap">{{ $entry->created_at->format('d/m/Y H:i:s') }}</td><td><code>{{ $entry->action }}</code></td><td>{{ class_basename($entry->subject_type ?? '') }} {{ $entry->subject_id ? '#'.$entry->subject_id : '' }}</td></tr>@empty<tr><td colspan="3" class="muted">Nessuna operazione registrata.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $rows->links() }}</div></section>

            @elseif($section === 'settings')
                <div class="two-column"><section class="panel"><div class="panel-heading"><h2>Architettura dell’installazione</h2><span class="badge">MVP</span></div><dl class="settings-list"><div><dt>Core applicativo</dt><dd>Laravel · Service condivisi</dd></div><div><dt>Archivio e retrieval</dt><dd>PostgreSQL + pgvector</dd></div><div><dt>Modello embedding</dt><dd>qwen3-embedding:0.6b</dd></div><div><dt>Runtime locale</dt><dd>Ollama</dd></div><div><dt>Interfacce</dt><dd>Dashboard · API interne · MCP</dd></div><div><dt>Generazione finale</dt><dd>Nelle rispettive app AI esterne</dd></div><div><dt>Chiavi provider AI</dt><dd>Non richieste</dd></div></dl></section><section class="panel form-panel"><h2>Configurazione operativa</h2><p class="muted">Gli indirizzi dei servizi, il modello e i parametri di retrieval si configurano nell’ambiente del server. Questa vista descrive l’architettura e non verifica la salute dei servizi.</p><div class="callout"><strong>Privacy per impostazione predefinita</strong><p>Dati isolati per account e scope. Le app AI accedono soltanto attraverso integrazioni autenticate.</p></div><div class="callout"><strong>Funzioni previste nelle prossime fasi</strong><p>Connector cloud con OAuth, condivisione fra utenti, import PDF/DOCX e retention avanzata.</p></div></section></div>
            @endif
            <footer class="page-footer"><span>ContextDock <span aria-hidden="true">·</span> Self-hosted context engine</span><span>I tuoi dati. Il tuo contesto.</span></footer>
        </main>
    </div>
</div>
