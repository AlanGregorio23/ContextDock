<x-layouts.app>
    <main class="login-shell">
        <section class="login-intro"><a href="/" class="brand"><span class="brand-mark" aria-hidden="true">C<span>·</span></span><span>ContextDock<small>SELF-HOSTED CONTEXT ENGINE</small></span></a><div><span class="eyebrow">IL TUO CONTESTO, SOTTO CONTROLLO</span><h1>La memoria resta.<br>Le app AI possono cambiare.</h1><p>Un unico spazio per memorie, decisioni e documenti. Retrieval locale, accesso controllato e Context Pack verificabili.</p><div class="login-tags"><span>Laravel</span><span>pgvector</span><span>Ollama</span><span>MCP</span></div></div><small>qwen3-embedding:0.6b · Nessuna API key di provider AI</small></section>
        <section class="login-form-area"><form method="POST" action="/login" class="panel form-panel login-form">@csrf<div><span class="eyebrow">BENTORNATO</span><h2>Accedi al tuo ContextDock</h2><p class="muted">Usa l’account creato durante l’installazione.</p></div>@if(session('status'))<div class="notice" role="status">{{ session('status') }}</div>@endif @if($errors->any())<div class="error-box" role="alert">@foreach($errors->all() as $error)<p>{{ $error }}</p>@endforeach</div>@endif<label for="email">Email</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="username" autofocus><label for="password">Password</label><input id="password" name="password" type="password" required autocomplete="current-password"><label class="checkbox-label"><input name="remember" type="checkbox" value="1">Ricordami su questo dispositivo</label><button class="button primary" type="submit">Accedi <span aria-hidden="true">→</span></button><small class="muted">L’accesso protegge i dati della tua installazione locale.</small></form></section>
    </main>
</x-layouts.app>
